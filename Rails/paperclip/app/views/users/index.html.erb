<div class="wrapper">
<% if @users %>
  <% @users.each do |user| %>
    <%= image_tag user.avatar.url %>
    <p>Uploaded by: <%= user.name %></p>
    <p><%= user.description %></p>
    <hr>
  <% end %>
<% end %>
<p><a href="/users/new">Submit a new Photo</a></p>
</div>
